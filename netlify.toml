[build]
  publish = "."
  # No build command needed — pure static HTML site.
  # Netlify deploys in ~5 seconds on every commit (no build minutes consumed).

[[headers]]
  for = "/admin/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
